<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $s = App\Models\TtsSpeechLine::where('speech_key', 'lesson-intro')->first(); $disk = Illuminate\Support\Facades\Storage::disk($s->audio_storage_disk); $audio = $disk->get($s->audio_storage_path); var_dump(hash('sha256', $audio)); var_dump($s->audio_sha256); var_dump(hash_equals($s->audio_sha256, hash('sha256', $audio)));
